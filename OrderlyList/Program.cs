using System;
using System.Collections.Generic;

namespace OrderlyList {
    class Program {
        static void Main(string[] args) {

            List<Employee> employees = new List<Employee>();
            employees.RecberId()



            Console.WriteLine();
            for (int i = 0; i < 1000; i++) {
                foreach (Employee cada in list) {
                    if (cada.Id == i) {
                        Console.WriteLine(cada);
                    }
                }
            }
        }
    }
}